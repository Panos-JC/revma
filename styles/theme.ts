import { createMuiTheme, ThemeOptions } from "@material-ui/core";

export const themeOptions = createMuiTheme({
  palette: {
    type: "light",
    primary: {
      main: "#323d77",
    },
    secondary: {
      main: "#bc1118",
    },
  },
});
